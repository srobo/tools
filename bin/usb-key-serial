#!/usr/bin/env python
# Displays the serial number of connected USB keys
import pyudev

con = pyudev.Context()

for dev in con.list_devices( subsystem = "usb", ID_DRIVE_THUMB="1" ):
    print dev["ID_SERIAL_SHORT"]

